services:
  php:
    build:
      context: dev/php
      args:
        HOST_USER_ID: ${USER_ID}
    container_name: avro-validator
    hostname: avro-validator
    tty: true
    volumes:
      - ../:/var/www/html
